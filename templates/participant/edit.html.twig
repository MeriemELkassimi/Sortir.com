{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} |Modification d'un participant{% endblock %}

{% block body %}
    <h1>Modification du participant</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_participant_index') }}">Retour à la liste</a>

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
