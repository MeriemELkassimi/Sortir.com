{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Etat{% endblock %}

{% block body %}
    <h1>Etat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat.id }}</td>
            </tr>
            <tr>
                <th>Libéllé</th>
                <td>{{ etat.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etat_index') }}">Retour à la liste</a>

    <a href="{{ path('app_etat_edit', {'id': etat.id}) }}">edit</a>

    {{ include('etat/_delete_form.html.twig') }}
{% endblock %}
