<form method="post" action="{{ path('app_etat_delete', {'id': etat.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat.id) }}">
    <button class="btnAppPerso">Supprimer</button>
</form>
