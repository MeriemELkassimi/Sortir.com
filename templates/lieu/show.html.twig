{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Lieu{% endblock %}

{% block body %}
    <h4>Lieu</h4>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lieu.nom }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ lieu.rue }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ lieu.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ lieu.longitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lieu_index') }}"><button class="btnAppPerso">
            Retour à la liste
        </button></a>

    <div>
        <a href="{{ path('app_lieu_edit', {'id': lieu.id}) }}"><button class="btnAppPerso">
            Modifier
        </button></a>
    </div>
    {{ include('lieu/_delete_form.html.twig') }}
{% endblock %}
