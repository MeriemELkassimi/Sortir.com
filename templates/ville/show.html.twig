{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Ville{% endblock %}

{% block body %}
    <h4>Ville</h4>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>Code postal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ville_index') }}"><button class="btnAppPerso">
            Retour à la liste
        </button></a>

    <div>
        <a href="{{ path('app_ville_edit', {'id': ville.id}) }}"><button class="btnAppPerso">
            Modifier
        </button></a>
    </div>

    {{ include('ville/_delete_form.html.twig') }}
{% endblock %}
